//import { useEffect, useState } from '@my-react/hooks';

export function UtilityComplex() {

	return (
		<main className="main">
			<h1>Utility Complex</h1>
			<p>Тестовая страница.</p>

			<button onClick={() => window.location.href = '/'}>Вернуться на главную</button>
		</main>
	);
}
